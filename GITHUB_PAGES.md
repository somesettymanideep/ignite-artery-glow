# GitHub Pages Deployment
# This site is built with TanStack Start.

To deploy to GitHub Pages:
1. Update `package.json` with your repo name in `build:gh-pages` if different.
2. Run `npm run build:gh-pages`.
3. Deploy the `dist/client` directory to the `gh-pages` branch.
